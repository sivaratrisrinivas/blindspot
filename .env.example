# Groq already lives in ~/.bashrc; repeated here only if you prefer per-project config
# GROQ_API_KEY=
# TENSORMUX_API_KEY serves the same two roles as Groq behind --provider tensormux
TENSORMUX_API_KEY=
NEATLOGS_API_KEY=
AO_PORT=3011
